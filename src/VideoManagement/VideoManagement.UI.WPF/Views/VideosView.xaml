<UserControl x:Class="VideoManagement.UI.WPF.Views.VideosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoManagement.UI.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListBox x:Name="Items" ItemsSource="{Binding Videos}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Width="Auto" Orientation="Horizontal">
                    <StackPanel>
                        <ToggleButton MinWidth="100" MinHeight="100" MaxHeight="100" MaxWidth="100" >
                            <ContentControl Content="{StaticResource searchIcon}"  />
                        </ToggleButton>
                    </StackPanel>
                    <StackPanel>
                        <Label Content="{Binding Name}"></Label>
                        <Label Content="{Binding Views}"></Label>
                    </StackPanel>
                    <StackPanel>
                        <Image Source="{StaticResource copyIcon}" Width="50" Height="50" />
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
